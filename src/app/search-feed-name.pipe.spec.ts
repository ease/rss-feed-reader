import { SearchFeedNamePipe } from './search-feed-name.pipe';

describe('SearchFeedNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFeedNamePipe();
    expect(pipe).toBeTruthy();
  });
});
